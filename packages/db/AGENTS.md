
## Implementation Rules
When adding new tables, always define them in /packages/db/src/schema.ts first. After defining a new schema, automatically suggest running npx drizzle-kit generate to keep migrations in sync.
